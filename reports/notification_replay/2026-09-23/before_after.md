# 2026-09-23 通知与价位整改对照

旧版自然日来自已读取的本机数据库与公开归档；新版结果来自隔离测试和冻结样本。两者证据等级不同，不能计算真实消息下降率或收益影响。

| 项目 | 旧版基线 | 新版隔离回放 | 未完成的自然日证据 |
|---|---|---|---|
| 批量事件门 | 变化事件计算后仍可能调用全池模型 | 20 只平静持仓 0 次模型；1 只变化 1 次批量模型 | 实际交易日模型耗时与触发分布 |
| 建议与通知 | `save_suggestion` 布尔返回，原建议可能直接触发通知 | 提案、Policy、决策、Outbox 同事务；只发批准且冻结事实匹配的动作 | 真实 Lark 正文与平台接受样本 |
| 未定方向 | 旧 watch/review 可能归入 HOLD | 内部 REVIEW，Policy 不授可执行动作 | 旧历史数据不可回写 |
| 东财 VWAP | “手”未经换算，样本会放大 100 倍 | 样本 12,967 手换成 1,296,700 股，VWAP 10.389054 元 | 实时供应商单位抽样 |
| 固定时钟与预热 | 逐 N 行聚合、样本不够也有 MA20 | 缺 09:33 标不完整；2 根 120m MA20 为 NULL | 竞价尾段自然日源差异 |
| 支撑失守 | 当前低点可覆盖前支撑 | 比较上一冻结支撑 20.30，当前 20.10 不抹去破位 | 自然日价位解释核对 |
| 红色集合去重 | 整组作为 key，A→A+B→A 易重复/漏升级 | 每持仓、计划版本、原因分别去重 | 实际多股并发风险样本 |
| 投递 | 尝试过即跳过，缺逐条账本 | PENDING/ACCEPTED/FAILED/UNKNOWN/EXPIRED/CANCELLED 可追溯 | 平台真实响应及用户已读仍未观测 |
| 执行 | 点击/登记易与券商成交混淆 | 请求幂等、累计限额、用户报告与后续对账分离，登记后提示剩余额度 | 券商流水及人工核对 |
| 公开归档 | 无通知逐条摘要 | 白名单追加匿名通知状态、hash 和质量标记 | 新版本真实自然日归档尚未发生 |

旧版公开 manifest 的 267 个工作流、539 条信号、0 条可执行信号、P10 `FAIL_CLOSED` 是真实已观察计数。旧版逐条飞书消息数、重复率、冲突率及用户已读为 `NOT_OBSERVED`，不可填 0。新版未调用真实模型、真实 Lark 或券商，也未覆盖今天已结束的交易日。

## 本地验证

- Python：`python -m pytest tests -q --disable-warnings --ignore=tests/test_makefile_windows.py --ignore=tests/test_sse_endpoints.py`，853 通过、1 跳过；SSE 定时测试曾偶发失败，单项重跑通过。完整套件另有 Windows 缺少 `make` 命令的环境失败，不能写成全量通过。
- 前端：`pnpm build` 通过；当前 Node 22.23.1 低于仓库声明的 24.14.0，但本次构建完成。
- 迁移：在在线 SQLite 备份的隔离副本上从版本 134 升至 138，三张新表存在；隔离副本测试后清理，运行库未迁移。
- 代码：`git diff --check` 通过；20 只合成持仓特征计算约 21.182 秒，只测本机计算段，不是生产端到端 P95。
