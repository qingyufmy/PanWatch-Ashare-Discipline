# OpenTelemetry 导出(可选依赖组)
#
# PanWatch 的自建可观测(log_context / agent_runs / progress)不依赖这些包;
# 仅当你想把 trace 导出到标准 APM(Jaeger / Tempo / Langfuse OTLP 等)时才需安装:
#
#   pip install -r requirements-otel.txt
#
# 安装后再配置环境变量 OTEL_EXPORTER_OTLP_ENDPOINT 即可启用(见 README「OTel 导出」)。
# 未安装或未配置 endpoint 时,src/core/otel.py 全程 no-op,零副作用。

opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0
