# 交易日公开运行归档

收盘后按 `YYYY-MM-DD/` 建立目录。每个日期目录包含 `manifest.json`、持仓快照摘要、工作流执行记录、批量计划、信号与规则判定、模型调用元数据、分钟数据覆盖、问题台账及 P10 门禁快照。

公开文件只包含经过字段筛选的运行结果，不含账户金额、持仓数量和成本、密钥、通知地址、原始模型文本或本地数据库。`REVIEW`、`MISSED`、`FAIL_CLOSED` 是真实运行状态，请勿当作验收通过。
