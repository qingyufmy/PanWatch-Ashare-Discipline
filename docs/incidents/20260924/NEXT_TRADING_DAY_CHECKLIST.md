# 下一交易日 Shadow 核查

1. 盘前核对服务进程加载的提交、数据库迁移版本 142、10 只用户声明持仓、交易日历、默认 Lark 配置和 `portfolio_discipline_mode`。用户声明不写作券商核对事实。
2. 08:50/恢复批次核对模型真实结束时刻、日计划、逐股信号和政策时钟；失败或迟到保持原样，不补写“准时”。
3. 10:00、13:30 核对市场证据的 `completed_at`、指数/持仓新鲜度、候选池范围、全球科技来源时间。风险观察应在模型调用前落库；即使整组行情不全或模型失败，已有合格单股风险也应有 `SUPPRESSED/SHADOW_ONLY` 裁决记录。模型输出晚于行情有效期应留 Issue，不写迟到决议。
4. 核对单股风险观察来源信号未过期、证据 ID 可解析、通知 ID 可解析、数量未获批准；确认对 Shadow ID 调用分发器返回 `SKIPPED/SUPPRESSED`。不重放历史消息。
5. 比较新旧持仓建议、冲突和撤销；没有个股技术证据时保持“待核查”，不能因全市场转弱自动清仓。核对可核实价位的 `level_id/source_asof/method/quality`、前后 Feature 和新鲜报价；页面每票只显示一条当前状态，历史在详情。
6. 盘后核对 Issue Ledger、时钟违例、工作流漏跑、P10 门禁和纸盘净值。收益样本与真实用户交易、券商对账分别统计；无数据保持 `null/UNPROVEN`。

通过条件是观察 100% 有通知裁决、引用完整、无未授权 Lark、无实盘订单、无反向时间戳。之后再评审人工建议与消息模板；不自动激活候选 Prompt/Policy/Model。
