# P0 与后续阶段验收门禁

## 本轮 P0

P0 完成需要：仓库提交与依赖锁定可复现；真实源码链和默认/运行配置分清；现有后端、包、前端测试结果逐项记录并分类；新增契约测试只记录现状；无生产代码、Prompt、交易阈值、调度和数据库迁移变更。任一项目不满足，P0 整体标记 `FAIL_CLOSED`，不能宣称文档 V1 已落地。

## P1–P10 的后续门禁

附件要求按 P1 真相/计划、P2 信号/执行、P3 PolicyGate、P4 模型路由、P5 Prompt、P6 分钟线、P7 日流程、P8 重放、P9 问题台账、P10 Shadow→PROD 顺序推进。每阶段应有独立变更、测试和兼容迁移。生产验收还需要真实持仓、数据 freshness、全覆盖 Hard Risk、完整追溯、T+1、EOD reconcile 与 Shadow 指标证据；本轮均未验证。未取得这些证据前，V1 PROD Gate 为 `FAIL_CLOSED`。
